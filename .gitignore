__pycache__/
.pytest_cache/
*.pyc
.env
*.log

# User-specific game paths, OBS passwords, and local links should not be published.
configs/*.json
!configs/example.json
launchers/*.bat

# Runtime play history and generated summaries.
logs/*.csv
!logs/.gitkeep
outputs/

# Config backups created before deletion — may contain OBS passwords and local paths.
backups/

# Local build outputs.
dist/
build/
*.spec
